import React from 'react';
import  Container  from './styles'

export const Button = ({ title, loading = false, ...rest }) => {
    return(
      <>
      <Container 
      type="button"
      > 
      </Container>
      </>
    )
  } 
